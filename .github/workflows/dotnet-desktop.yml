name: .NET CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '8.0.x'

    - name: Restore dependencies
      run: |
        dotnet restore CalculatorApp/CalculatorApp.csproj
        dotnet restore CalculatorApp.Tests.csproj

    - name: Build
      run: |
        dotnet build CalculatorApp/CalculatorApp.csproj --no-restore
        dotnet build CalculatorApp.Tests.csproj --no-restore

    - name: Test
      run: dotnet test CalculatorApp.Tests.csproj --no-restore
